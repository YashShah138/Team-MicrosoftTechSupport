import getWindowScroll from "@popperjs/core/lib/dom-utils/getWindowScroll.d.ts";
import getWindow from "@popperjs/core/lib/dom-utils/getWindow.d.ts";
import { isHTMLElement } from "@popperjs/core/lib/dom-utils/instanceOf.d.ts";
import getHTMLElementScroll from "@popperjs/core/lib/dom-utils/getHTMLElementScroll.d.ts";
export default function getNodeScroll(node) {
  if (node === getWindow(node) || !isHTMLElement(node)) {
    return getWindowScroll(node);
  } else {
    return getHTMLElementScroll(node);
  }
}